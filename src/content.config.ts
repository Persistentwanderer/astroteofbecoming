// Content now lives in Sanity rather than local content collections.
// This file is intentionally left with no collections; queries are made
// through the Sanity client (see src/lib/sanity).

export const collections = {};
